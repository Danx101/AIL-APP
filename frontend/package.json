{
  "name": "abnehmen-frontend",
  "version": "1.0.0",
  "description": "Frontend for Abnehmen im Liegen appointment system",
  "main": "src/app.js",
  "scripts": {
    "dev": "live-server --port=3000 --host=localhost public/",
    "start": "live-server --port=3000 --host=localhost public/",
    "build": "echo 'Build process not implemented yet'",
    "test": "jest",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "keywords": ["frontend", "appointment", "scheduling"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.3.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "jest": "^29.7.0",
    "eslint": "^8.54.0"
  }
}